name: python
description: Python 3 development environment
depends_on:
  - base
priority: 50

dockerfile: |
  # Python development
  RUN dnf install -y \
      python3 python3-pip python3-devel \
      && dnf clean all
